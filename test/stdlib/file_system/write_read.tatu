; Test write and read file in temp

(block
  (var temp-path (str:concat (fs:temp-dir) "/tatu-test-write.txt"))
  (fs:write temp-path "test content")
  (var result (fs:read temp-path))
  (fs:delete temp-path)
  result)

; Expect: test content
