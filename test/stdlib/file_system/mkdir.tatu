; Test create directory

(begin
  (var temp-dir (str:concat (fs:temp-dir) "/tatu-test-mkdir"))
  (fs:mkdir temp-dir)
  (var result (fs:is-dir temp-dir))
  (fs:delete temp-dir)
  result)

; Expect: true
