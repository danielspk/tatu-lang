; Test avoid multiple eval

(macro square (x) (block (var __t x) (* __t __t)))

(var counter 0)

(def next ()
  (block
    (set counter (+ counter 1))
    counter))

(var result (square (next)))

(+ (* result 10) counter)

; Expect: 11
