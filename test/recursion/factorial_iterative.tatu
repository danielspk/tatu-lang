; Test iterative factorial

(block
  (def factorial (x)
    (block
      (var result x)
      (while (> x 1)
        (block
          (set x (- x 1))
          (set result (* result x))))))

  (factorial 5)
)

; Expect: 120
